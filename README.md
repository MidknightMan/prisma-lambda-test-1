## NODE_TS_SERVER_PRISMA_TEMPLATE

To use this server, simply click the 'Use Template' button on GitHub, change the name in the `package.json` file to be the name of the new server, run `npm install` and crack on!
